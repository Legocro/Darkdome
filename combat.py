#Functions for combat and experience

from classes import Character
from classes import Human
from random import *

def cb:
	if self.health > 0:
		pdmg = (self.strength * (randint(7, 12)) * 10

def cbxp:
	cbxp = (pdmg * 40) / 120

#Move all cb related functions here